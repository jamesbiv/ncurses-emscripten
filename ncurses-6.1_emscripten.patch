diff -Naur ncurses-6.1/ncurses/curses.priv.h ncurses-6.1_emscripten/ncurses/curses.priv.h
--- ncurses-6.1/ncurses/curses.priv.h	2018-01-20 23:13:41.000000000 -0300
+++ ncurses-6.1_emscripten/ncurses/curses.priv.h	2019-07-18 09:25:06.551150164 -0300
@@ -64,6 +64,23 @@
 #include <stddef.h>		/* for offsetof */
 #include <stdlib.h>
 #include <string.h>
+
+#ifdef __EMSCRIPTEN__ 
+#ifdef getenv
+#undef getenv
+#endif
+
+/* Declaring getenv() as per https://github.com/emscripten-core/emscripten/issues/6766 */
+#define getenv(name) (char*)({\
+\
+  char* volat = getenv(name);\
+  int vlen = strlen(volat) + 1;\
+  char* dest = (char*)malloc(vlen * sizeof(char));\
+  strcpy(dest, volat);\
+  dest;\
+})
+#endif
+
 #include <sys/types.h>
 #include <sys/stat.h>
 
